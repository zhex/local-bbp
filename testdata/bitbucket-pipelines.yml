pipelines:
  default:
    - step:
        name: cache
        script:
          - echo "Hello, World"
          - cat node_modules/test.txt
        caches:
          - node

  custom:
    test:
      - step:
          script:
            - export FOO=bar
            - echo "Hello, Custom"
            - echo "FOO is $FOO"

