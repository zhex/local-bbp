pipelines:
  default:
    - step:
        name: build
        script:
          - ls

  custom:
    test:
      - step:
          script:
            - export FOO=bar
            - echo "Hello, Custom"
            - echo "FOO is $FOO"

